sudo: required

group: edge

services:
  - docker

install:
  - docker build --tag bodsch/docker-smashing-icinga2 .
  - docker run --detach --name smashing-icinga2 bodsch/docker-smashing-icinga2

script:
  - docker ps | grep -q smashing-icinga2

